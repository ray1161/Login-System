This is a login system, containing basic PHP+MySQL
It can update database, and check database;
It also includes two level of users, admin and normal users;
Admin can check all orders while normal users can only see their own orders;